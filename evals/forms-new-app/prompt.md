---
description: A new-app form-library question should pick TanStack Form + zod
tags: [defaults, forms]
allowed_tools: [Read, Glob, Grep, Skill]
---

I'm starting a new React web app. It has a multi-step signup form whose validation should match the API's input types. Which form library should I use? Keep it to a short recommendation.
